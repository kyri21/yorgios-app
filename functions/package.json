{
  "name": "yorgios-functions",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": "22"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "@types/nodemailer": "^7.0.11",
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^7.1.0",
    "googleapis": "^144.0.0",
    "nodemailer": "^8.0.1",
    "twilio": "^5.3.0"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
